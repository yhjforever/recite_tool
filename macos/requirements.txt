pypdf>=4.0
requests>=2.28
PyYAML>=6.0
# 可选：解析 .pptx 原生课件（不装则仅支持 PDF/txt/md）
python-pptx>=0.6.21
# 可选：联网核对默认用 DuckDuckGo（provider=ddg 时需要；tavily/serper/bing 不需要此包）
ddgs>=6.0
# 界面三级回退：① Web 版（pywebview） ② CustomTkinter ③ ttkbootstrap / clam
pywebview>=5.0
# Python.NET 只用于 Windows WebView2；macOS 安装时不需要。
pythonnet>=3.0; sys_platform == "win32"
customtkinter>=5.2
ttkbootstrap>=1.10
