pypdf>=4.0
requests>=2.28
PyYAML>=6.0
# 可选：解析 .pptx 原生课件（不装则仅支持 PDF/txt/md）
python-pptx>=0.6.21
# 可选：联网核对默认用 DuckDuckGo（provider=ddg 时需要；tavily/serper/bing 不需要此包）
ddgs>=6.0
# 现代化界面：优先 CustomTkinter（圆角卡片），缺失则回退 ttkbootstrap，再缺回退内置 clam
customtkinter>=5.2
ttkbootstrap>=1.10
